PySide6>=6.11
PyMuPDF>=1.27
pikepdf>=10
Pillow>=10        # 画像処理（傾き補正など）
numpy>=1.26       # 傾き補正の射影プロファイル計算

# OCR は PyMuPDF 内蔵の Tesseract を使うため追加の pip パッケージは不要。
# 言語データ(*.traineddata)のみ必要で、アプリが自動で
#   %LOCALAPPDATA%\pdfeditor\tessdata
# に用意する（不足分は tessdata_fast から自動ダウンロード）。
# 任意: Tesseract 本体(UB-Mannheim版)を入れておくと eng/osd を流用できる。
